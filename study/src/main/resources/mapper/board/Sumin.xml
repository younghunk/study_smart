<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE  mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Sumin">
    <select id="boardSelectAll" resultType="com.example.demo.board.chosumin.vo.SMBoardVO">
        SELECT * FROM Board.tbl_board
        ORDER BY groupno, depthno asc
    </select>

    <insert id="boardInsert" parameterType="com.example.demo.board.chosumin.vo.SMBoardVO">
        <selectKey resultType="int" keyProperty="groupno" order="BEFORE">
            SELECT AUTO_INCREMENT
            from information_schema.tables
            WHERE table_name = 'tbl_board'
            AND table_schema=DATABASE();
        </selectKey>
        INSERT INTO Board.tbl_board (userid, subject, content, date, groupno)
        VALUES (#{userid}, #{subject}, #{content}, #{date}, #{groupno})
    </insert>

    <select id="boardSelect" parameterType="int" resultType="com.example.demo.board.chosumin.vo.SMBoardVO">
        SELECT * FROM Board.tbl_board
        WHERE seq = #{seq}
    </select>

    <update id="boardUpdate" parameterType="com.example.demo.board.chosumin.vo.SMBoardVO">
        UPDATE Board.tbl_board
        SET
            subject = #{subject},
            content = #{content}
        WHERE
            seq = #{seq}
    </update>

    <insert id="replyInsert" parameterType="com.example.demo.board.chosumin.vo.SMBoardVO">
        INSERT INTO Board.tbl_board (userid, subject, content, date, groupno, fk_seq, depthno)
        VALUES (#{userid}, #{subject}, #{content}, #{date}, #{groupno}, #{fk_seq}, #{depthno})
    </insert>
</mapper>